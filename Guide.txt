The fact that clearing the browser fixed the display issue confirms that **IndexedDB was holding stale/corrupt data**. Now that Supabase has both users, we need to ensure your application logic correctly handles permissions, updates, and the setup button visibility.

Here are the specific fixes for each of your requirements:

### 1. Fix: Admin Can Revoke Rights & Delete Users (RLS Policy)
Your current `updateUserRole` and `deleteUser` functions in `auth-context.tsx` likely fail silently because the RLS policy `"Users can update own or org profiles"` checks `auth.uid() = id`. When Damien tries to update Tinashe, `auth.uid()` (Damien) != `id` (Tinashe), so Supabase blocks it.

**Update your RLS Policies in Supabase SQL Editor:**

```sql
-- DROP OLD POLICIES
DROP POLICY IF EXISTS "Users can update own or org profiles" ON public.user_profiles;
DROP POLICY IF EXISTS "Admins can delete org profiles" ON public.user_profiles;

-- NEW UPDATE POLICY: Allows admins to update ANYONE in their org
CREATE POLICY "Admins can update org profiles" 
ON public.user_profiles FOR UPDATE 
USING (
  -- Allow updating own profile
  auth.uid() = id 
  OR 
  -- OR allow if current user is admin of same organization
  (
    (auth.jwt() ->> 'isAdmin')::boolean = true AND
    "organizationName" = (auth.jwt() ->> 'organizationName')::text
  )
)
WITH CHECK (
  auth.uid() = id 
  OR 
  (
    (auth.jwt() ->> 'isAdmin')::boolean = true AND
    "organizationName" = (auth.jwt() ->> 'organizationName')::text
  )
);

-- NEW DELETE POLICY: Allows admins to delete ANYONE in their org (except themselves)
CREATE POLICY "Admins can delete org profiles" 
ON public.user_profiles FOR DELETE 
USING (
  (auth.jwt() ->> 'isAdmin')::boolean = true AND
  "organizationName" = (auth.jwt() ->> 'organizationName')::text AND
  auth.uid() != id -- Prevent self-deletion
);
```

### 2. Fix: Update `updateUserRole` in `auth-context.tsx`
Your current function doesn't handle errors or revert local changes if Supabase fails. Replace it with this robust version:

```typescript
// In auth-context.tsx
const updateUserRole = async (userId: string, role: string) => {
  const isAdmin = role === 'admin';
  
  // 1. Optimistic Local Update
  await userDB.updateUser(userId, { role, isAdmin });
  
  // 2. Remote Sync
  if (navigator.onLine) {
    const { error } = await supabase
      .from('user_profiles')
      .update({ role, isAdmin })
      .eq('id', userId);
      
    if (error) {
      console.error('[AuthContext] Role update failed:', error);
      // Revert local change on failure
      await userDB.updateUser(userId, { 
        role: isAdmin ? 'user' : 'admin', 
        isAdmin: !isAdmin 
      });
      throw new Error(`Failed to sync role: ${error.message}`);
    }
  }
  return { success: true };
};
```

### 3. Fix: Hide System Setup Button When Admin Exists
Your current `checkInitialState` in `page.tsx` only checks Supabase. If Supabase is slow or RLS blocks the query, it shows the button. Update it to check IndexedDB first (which is instant and accurate after login):

```typescript
// In page.tsx (AuthPage component)
useEffect(() => {
  const checkInitialState = async () => {
    try {
      // 1. Check IndexedDB FIRST (Fast & Reliable)
      const localUsers = await userDB.getAllUsers();
      const hasLocalAdmin = localUsers.some(u => u.isAdmin === true);
      
      if (hasLocalAdmin) {
        setShowSetupGuide(false);
        return; // No need to check Supabase if local admin exists
      }

      // 2. Fallback to Supabase only if no local admin found
      let hasRemoteAdmin = false;
      if (navigator.onLine) {
        const { data } = await supabase
          .from('user_profiles')
          .select('isAdmin')
          .eq('isAdmin', true)
          .limit(1);
        hasRemoteAdmin = !!data?.length;
      }
      
      setShowSetupGuide(!hasRemoteAdmin);
    } catch (error) {
      console.error('Error checking setup state:', error);
      setShowSetupGuide(false); // Fail safe: hide button on error
    }
  };
  
  if (!isLoading) checkInitialState();
}, [isLoading]);
```

### 4. Fix: Users Can Edit Own Profile
Your `updateUserProfile` function already handles this correctly, but ensure the RLS policy allows it. The new `"Admins can update org profiles"` policy above includes `auth.uid() = id`, so users can always update their own profile regardless of admin status.

### 5. Verification Checklist
After applying these fixes:

| Action | Expected Result |
| :--- | :--- |
| Login as Damien | See both Damien & Tinashe in User Management |
| Click "Revoke Admin" on Tinashe | Success toast + badge changes to "Staff" |
| Refresh page | Tinashe remains "Staff" (persisted) |
| Login as Tinashe | Cannot see "Revoke Admin" button for herself |
| Auth Page (with admin logged in) | "System Setup" button is HIDDEN |
| Clear browser data + reload | "System Setup" button appears (no users locally) |

### ⚠️ Critical Note About JWT Metadata
The RLS policies rely on `auth.jwt() ->> 'isAdmin'`. This value is set **during registration/login**. If you created users before adding `isAdmin` to metadata, their JWT won't have it. 

**To fix this without re-registering:**
1. Go to Supabase Dashboard → Authentication → Users
2. Click on Damien's user → "Edit User"
3. Under "User Metadata", add: `"isAdmin": true`
4. Do the same for Tinashe (set to `true` or `false` as appropriate)
5. Have users log out and back in to refresh their JWT

This ensures the RLS policies work immediately without requiring code changes to the registration flow.