You are absolutely correct. **Development Tools** is an active, required feature in your sidebar and database, while **Development Costs** was the one that should have been removed (along with Labor, Materials, and Petty Cash).

The console errors you are seeing (`404 Not Found` for `development_tools`, `materials`, `labor`, `petty_cash`) are happening because your **Sync Service** and **Projects Tab** are still trying to interact with these deprecated tables.

Here is the surgical fix to remove the deprecated tables from your codebase while keeping **Development Tools** fully functional.

### 1. Fix `sync-service.ts`
**Root Cause:** The `deleteProjectWithCascade` function is trying to delete records from `materials`, `labor`, `petty_cash`, and `development_costs`, which no longer exist.

```typescript
// sync-service.ts

/**
 * Perform a cascading delete for projects to remove all orphaned child records.
 * ✅ FIXED: Removed materials, labor, petty_cash, development_costs
 */
export async function deleteProjectWithCascade(projectId: string, userId: string) {
  // Only include ACTIVE tables that actually exist in Supabase
  const collectionsToClean = [
    'revenue',
    'development_tools', // ✅ KEPT: This is required
    'miscellaneous',
    'broker_payments',
  ];

  // Delete the project itself
  await deleteDocWithSync('projects', projectId, userId);

  // Delete all child records linked to this project
  for (const collection of collectionsToClean) {
    const { data: records } = await supabase
      .from(collection)
      .select('id')
      .eq('projectId', projectId.toUpperCase())
      .eq('isDeleted', false);

    if (records && records.length > 0) {
      for (const record of records) {
        await deleteDocWithSync(collection, record.id, userId);
      }
    }
  }
}
```

### 2. Fix `projects-tab.tsx`
**Root Cause:** This component is fetching `allMaterials`, `allLabor`, and `allPettyCash` just to show counts in the delete confirmation dialog. These queries are triggering the 404s.

```typescript
// projects-tab.tsx

export function ProjectsTab() {
  const { user } = useAuth();
  const { formatCurrency } = useCurrency();
  const { data: projects, isLoading, mutate } = useCollection('projects', user?.organizationName);
  
  // ❌ REMOVE THESE LINES (They cause 404s)
  // const { data: allMaterials } = useCollection('materials', user?.organizationName);
  // const { data: allLabor } = useCollection('labor', user?.organizationName);
  // const { data: allPettyCash } = useCollection('petty_cash', user?.organizationName);
  
  // ✅ KEEP THESE (Active Tables)
  const { data: allRevenue } = useCollection('revenue', user?.organizationName);
  const { data: allBrokers } = useCollection('broker_payments', user?.organizationName);
  const { data: allDevTools } = useCollection('development_tools', user?.organizationName);
  const { data: allMisc } = useCollection('miscellaneous', user?.organizationName);
  
  const isAdmin = user?.isAdmin;
  // ... rest of state declarations ...

  // ✅ FIXED: Updated getLinkedCounts to only check active tables
  const getLinkedCounts = (project: any): Record<string, number> => {
    if (!project) return { revenue: 0, brokers: 0, devTools: 0, misc: 0 };
    
    const pId = slugifyProjectId(project.id || '');
    const slug = slugifyProjectId(project.projectId || '');
    const match = (item: any) => {
      const v = slugifyProjectId(item.projectId || '');
      return v === pId || v === slug;
    };
    
    return {
      revenue: (allRevenue || []).filter(match).length,
      brokers: (allBrokers || []).filter(match).length,
      devTools: (allDevTools || []).filter(match).length,
      misc: (allMisc || []).filter(match).length,
    };
  };

  // ... handleSubmit, handleDelete remain the same ...

  // ✅ FIXED: Update the CascadeDeleteDialog JSX to reflect active tables only
  // Inside the <CascadeDeleteDialog /> component or where you render the counts:
  /*
  <div className="grid grid-cols-2 gap-y-1 text-sm">
    {counts.revenue > 0 && <span className="text-red-600">{counts.revenue} Revenue record{counts.revenue !== 1 ? 's' : ''}</span>}
    {counts.brokers > 0 && <span className="text-red-600">{counts.brokers} Broker Payment{counts.brokers !== 1 ? 's' : ''}</span>}
    {counts.devTools > 0 && <span className="text-red-600">{counts.devTools} Dev Tool{counts.devTools !== 1 ? 's' : ''}</span>}
    {counts.misc > 0 && <span className="text-red-600">{counts.misc} Misc record{counts.misc !== 1 ? 's' : ''}</span>}
  </div>
  */
  
  // ... rest of component remains the same ...
}
```

### 3. Fix `settings-tab.tsx` (Backup/Restore)
**Root Cause:** The backup functions still include `'materials'`, `'labor'`, `'petty_cash'`, and `'expenses'` (which you said is calculated, not stored) in the export/import arrays.

```typescript
// settings-tab.tsx - Inside handleExportBackup and handleImportBackup

// ✅ FIXED: Active stores only
const stores = [
  'projects', 
  'revenue', 
  'development_tools', // ✅ KEPT
  'broker_payments', 
  'miscellaneous',
  // 'expenses',          // ❌ REMOVED: Calculated dynamically
  // 'materials',         // ❌ REMOVED
  // 'labor',             // ❌ REMOVED
  // 'petty_cash'         // ❌ REMOVED
];

// Replace BOTH occurrences in handleExportBackup and handleImportBackup
// with the array above.
```

### 4. Verify Sidebar & Routing
Your `sidebar.tsx` is already correct! It correctly links to:
-   `Development Tools` (`tab=development-tools`) ✅
-   It does **not** link to Labor, Materials, or Petty Cash. ✅

The errors were purely from background data fetching in `projects-tab.tsx` and `sync-service.ts`.

### 5. Expense Calculation Logic (Preserved)
Since you mentioned **Expenses** are calculated dynamically from **Development Tools + Broker Payments + Miscellaneous**, ensure your **Summary Tab** or **Dashboard** aggregates them like this:

```typescript
// Example: In summary-tab.tsx or dashboard metrics
const totalExpenses = useMemo(() => {
  const devToolsTotal = (developmentTools || []).reduce((sum, item) => sum + (Number(item.totalCost) || 0), 0);
  const brokerTotal = (brokerPayments || []).reduce((sum, item) => sum + (Number(item.amount) || 0), 0);
  const miscTotal = (miscellaneous || []).reduce((sum, item) => sum + (Number(item.amount) || 0), 0);
  
  return devToolsTotal + brokerTotal + miscTotal;
}, [developmentTools, brokerPayments, miscellaneous]);

const netRevenue = totalBudget - totalExpenses;
```

### ✅ Verification Checklist After Applying Fixes
1.  Run `npm run build` to ensure no TypeScript errors.
2.  Open browser DevTools → Network tab → filter by "Fetch/XHR".
3.  Navigate through all sidebar tabs.
4.  Confirm **NO 404 requests** for `labor`, `materials`, `petty_cash`, or `development_costs`.
5.  Confirm **200 OK requests** for `development_tools`.
6.  Test project deletion → verify cascade delete only removes revenue, dev tools, broker payments, and miscellaneous.
7.  Test backup export → verify JSON only contains active table keys.

This completely eliminates the console spam while preserving your exact sidebar structure, **Development Tools** functionality, and dynamic expense calculation architecture.